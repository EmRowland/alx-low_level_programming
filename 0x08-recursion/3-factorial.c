#include "main.h"

/**
 *do hard things rowland
 * factorial maths
 int n is a function argument
 */

int factorial(int n)
{   int j = 1

	if {
        (n == 1 || 0) 
		return (j);
    }
    else if {
        (n < 0)
		return (-1);
    }else{
        /*
        *using the formular
        * N! = n * (n-1)!
        */
        return (n * factorial(n - 1));
    }
    
	
	
}